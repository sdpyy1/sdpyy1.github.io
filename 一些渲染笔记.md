# 渲染

## 虎书上一些东西

### 辐射度量学

1. **辐射能量**（符号：Q，单位：焦耳）：就理解物理的做功的数值，这里指电磁辐射的能量
2. **功率、辐射通量**（符号：Φ，单位：(瓦特)或者(流明)）：有做功，当然就有功率，单位时间的做功呗
3. **辐射强度**（Radiant Intensity）：指光源发出的每单位立体角上的功率，这就是把功率又进一步细分，细分到立体角。 辐射强度 = 功率 / 4Pi，这一概念并不考虑光源面积
4. **辐照度**（Irradiance）：是指每单位照射面积所接收到的功率（瓦特、流明），这里主要如何单位面积不垂直，要乘以$cos\theta$
5. **辐射**（Radiance）：每单位立体角、每单位垂直面积的功率（瓦特、流明），不仅考虑单位立体角，还考虑单位面积

Irradiance是单位面积的功率，Radiance进一步是单位面积的单位立体角的功率。

**结合虎书的例子理解Irradiance:**,假设点光源的功率是1，把它放在一个单位球体的圆心，球的面积为4Pi，所以1/4Pi就是单位面积的功率，也就是Irradiance（看书一会就解决了这个问题）

解决Irradiance计算是解决了多少光落在了着色点，但物体怎么反射它，取决于着色点的材质，用BRDF来描述。**所以着色计算本质就是计算（1）计算Irradiance表达有多少光能被反射（2）计算BRDF表达着色点如何反射**

## 图形API

### 坐标问题（最佳参考:https://zhidao.baidu.com/question/566189338660400772.html）

1. 世界坐标系：OpenGL、Vulkan是右手系，DirectX是左手系。 （但是也有说在渲染流程走到NDC之前，我们其实不需要考虑不同API的差异问题。世界空间、。。模型空间、相机空间等都是用户自定义的，与图形API无关）
2. NDC->屏幕坐标：（NDC->屏幕坐标这个过程，每个API都有自己的定义，所以用户的透视矩阵需要满足各个API的要求才能让他正确映射到屏幕坐标）Vulkan映射时Y轴是朝下的。  DirectX、Vulkan的z值映射为[0,1]，OpenGL是[-1,1]。 适配不同API的做法是调整透视投影矩阵